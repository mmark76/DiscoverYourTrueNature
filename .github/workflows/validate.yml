name: Validate prototype

on:
  push:
    branches:
      - main
      - agent/mvp-assessment-prototype
  pull_request:

jobs:
  typecheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.0

      - name: Install dependencies
        run: npm install

      - name: Type-check
        run: npm run typecheck
