# Shared

Reusable code used by more than one feature belongs here.

Possible categories include shared components, styles, types, constants, and utilities. Code used by only one feature should remain inside that feature until reuse is proven.
