# App

Application composition, startup, routing, and top-level configuration belong here.

Business logic must remain inside the relevant feature rather than accumulating in the application entry point.
